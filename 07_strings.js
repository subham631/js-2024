const name = "subham";
const age = 20;

console.log(name + age + 5);  // not recommended

console.log(`My name is ${name} and my age is ${age}`);

const subject = new String("Geography");

console.log(subject.toLowerCase());
console.log(subject.length);


// go to the console in browser and check all the functions of strings
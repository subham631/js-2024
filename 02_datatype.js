"use strict"; // treat all js code as newer version
//code readability is the first preference
//alert(1+1) // We are using nodejs, not browser

let name = "Subham";
let age = 20;
let isGraduate = false;
let temp = null;
//undefined => when you don't assign the value
//symbol => unique (more use in react)

// object
console.log(typeof name); 
console.log(typeof undefined); // undefined
console.log(typeof null); // object